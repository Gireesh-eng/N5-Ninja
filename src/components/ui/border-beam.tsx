import React from 'react';

export const BorderBeam = ({ size, duration, delay }: { size: number, duration: number, delay: number }) => {
  return (
    <div style={{ width: size, height: size, border: '1px solid black' }}>
      {/* Placeholder for BorderBeam */}
    </div>
  );
};
